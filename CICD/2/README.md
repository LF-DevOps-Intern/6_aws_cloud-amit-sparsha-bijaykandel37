In this directory, we have react-scripts folder, which contains old 
screenshots. In this process, policy is updated manually which was not considered
as fully automated.

So another directory react-scripts-redone contains actual solution with 
complete automation script.
